\documentclass[11pt]{article}

% ---------------------------------------------------------------------------
% KLA / Applied Materials SEMICON India Hackathon 2026
% Drift-Sense Reference Localization - Method and Design Document
% Compile with: pdflatex model_architecture.tex  (run twice for references)
% ---------------------------------------------------------------------------

\usepackage[a4paper,margin=2.3cm]{geometry}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{array}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{enumitem}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds,calc}
\usepackage[hidelinks]{hyperref}
\usepackage{titlesec}

\definecolor{klablue}{RGB}{18,72,132}
\definecolor{klateal}{RGB}{0,132,140}
\definecolor{klagrey}{RGB}{95,105,115}
\definecolor{panel}{RGB}{240,244,248}
\definecolor{accent}{RGB}{214,93,20}

\titleformat{\section}{\Large\bfseries\color{klablue}}{\thesection}{0.6em}{}
\titleformat{\subsection}{\large\bfseries\color{klateal}}{\thesubsection}{0.6em}{}

\newcommand{\term}[1]{\textbf{\color{klablue}#1}}

\setlength{\parskip}{0.5em}
\setlength{\parindent}{0pt}

\begin{document}

% ============================ TITLE ============================
\begin{titlepage}
\centering
\vspace*{1.5cm}
{\color{klablue}\rule{\textwidth}{2pt}}\\[0.6cm]
{\Huge\bfseries\color{klablue} Finding a Landmark on a\\[0.2cm] Semiconductor Wafer}\\[0.5cm]
{\Large\color{klagrey} Reference Localization for the Applied Materials ``Drift-Sense'' Task}\\[0.8cm]
{\color{klateal}\rule{0.6\textwidth}{1pt}}\\[0.6cm]
{\large KLA / Applied Materials SEMICON India Hackathon 2026}\\[0.3cm]
{\large Method and Design Document}\\[1.2cm]

\begin{minipage}{0.85\textwidth}
\centering\itshape\color{klagrey}
This document explains, in plain language, how our system takes a small
high-resolution ``reference'' patch of a wafer and finds exactly where it
sits inside a larger, blurry ``search'' image --- what the method does, why
it is deliberately simple, and what we discovered about the limits of the
task itself.
\end{minipage}

\vfill
{\color{klagrey}\small All quantitative results in this document were produced on a
\emph{synthetic} dataset from the official Drift-Sense generator, run
locally. They are labelled synthetic throughout, are not tuned on any hidden
test set, and do not describe official KLA/Applied Materials performance.}\\[0.4cm]
{\color{klablue}\rule{\textwidth}{2pt}}
\end{titlepage}

\tableofcontents
\newpage

% ============================ 1. PROBLEM ============================
\section{The problem in one page}

Imagine you have a very detailed close-up photograph of one small feature on
a wafer, and a second, wider photograph of the same area that is blurrier and
has been nudged sideways by an unknown amount (``drift''). Your job is to
point to the exact spot in the wide photo where the close-up was taken.

Concretely, the organisers give us two images:

\begin{itemize}[leftmargin=1.4em]
  \item A \term{Reference} image: $1000\times1000$ pixels at $1$~nm per pixel.
        It shows a $1~\mu\text{m}\times1~\mu\text{m}$ field of view in high detail.
  \item A \term{Search} image: $1000\times1000$ pixels at $10$~nm per pixel.
        It shows a $10~\mu\text{m}\times10~\mu\text{m}$ field of view --- ten
        times wider --- and it is degraded (noisy and blurred).
\end{itemize}

Because the Search image covers ten times more ground in the same number of
pixels, \term{the whole Reference fits inside roughly a $100\times100$ pixel
window of the Search image}. We must output the pixel coordinates
$(x,y)$ where the Reference's field of view is centred.

\term{Success is measured by one number}: the Euclidean pixel distance
between our predicted centre and the true centre. Smaller is better. This is
a \emph{localization} task --- we return a coordinate, not a cleaned-up
picture. There is no PSNR, no SSIM, no denoising involved.

\begin{center}
\begin{tikzpicture}[node distance=8mm,>=Latex,font=\small]
  \node[draw,rounded corners,fill=panel,minimum height=1.4cm,minimum width=2.4cm] (ref) {\shortstack{Reference\\ (sharp, 1 nm/px)}};
  \node[draw,rounded corners,fill=klablue!14,right=1.8cm of ref,minimum height=1.4cm,minimum width=2.6cm] (model) {Our matcher};
  \node[draw,rounded corners,fill=accent!18,right=1.8cm of model,minimum height=1.4cm,minimum width=2.6cm] (out) {\shortstack{Centre $(x,y)$\\ in Search px}};
  \node[draw,rounded corners,fill=panel,below=1.4cm of model,minimum height=1.4cm,minimum width=2.6cm] (srch) {\shortstack{Search\\ (blurry, 10 nm/px)}};
  \draw[->] (ref) -- (model);
  \draw[->] (srch) -- (model);
  \draw[->] (model) -- (out);
\end{tikzpicture}
\end{center}

% ============================ 2. KEY IDEA ============================
\section{The core idea: slide the small picture over the big one}
\label{sec:ncc}

The Reference and the Search show the \emph{same} physical structure, just at
different pixel sizes. So the natural way to find the match is to \term{shrink
the Reference down to the Search's scale and slide it across the Search image,
looking for the position where the two line up best}. This classic technique
is called \term{template matching}.

The three steps are:

\begin{enumerate}[leftmargin=1.6em]
  \item \term{Resize} the $1000\times1000$ Reference down to about
        $100\times100$ pixels, so it is at the same scale as the Search.
        (We try a few sizes around $10\times$ smaller, because a real solver
        is not handed the exact ratio.)
  \item \term{Slide} this small template over every position of the Search
        image and, at each position, compute how well they match using
        \term{normalized cross-correlation} (NCC). ``Normalized'' means the
        score ignores overall brightness and contrast and looks only at the
        \emph{pattern}, so it is robust to the Search image being darker,
        brighter, or noisier.
  \item \term{Pick the peak}: the position with the highest correlation score
        is our answer. We then refine it to \term{sub-pixel} accuracy by
        fitting a small parabola to the scores around the peak (the true peak
        usually sits between two pixels).
\end{enumerate}

\[
  (x^\star,y^\star) \;=\; \arg\max_{(x,y)}\;
  \mathrm{NCC}\big(\text{Search around }(x,y),\;\text{resized Reference}\big)
\]

\subsection*{Why this, and not a big neural network?}
This is the central design decision, and \term{we made it by measurement, not
by fashion}. Normalized cross-correlation is:
\begin{itemize}[leftmargin=1.4em]
  \item \term{Exact when it works.} On the samples it gets right, it is
        accurate to about \emph{one pixel} --- effectively perfect for this
        metric (see Section~\ref{sec:results}).
  \item \term{Tiny and fast.} It needs only NumPy and OpenCV, no GPU, no
        training, no internet. A prediction takes a fraction of a second.
  \item \term{Transparent.} There are no learned weights to distrust; anyone
        can read the fifty lines of code and see exactly what it does.
\end{itemize}
The real question is not ``can a bigger model be more accurate on the easy
cases'' --- nothing can beat one-pixel accuracy --- but ``can any model fix
the hard cases''. Section~\ref{sec:ceiling} shows the answer is \emph{no}, and
explains why. That finding is what justifies staying simple.

% ============================ 3. PIPELINE DETAILS ============================
\section{How the matcher works, step by step}

\subsection{Trying several scales}
We do not assume the pixel-size ratio is exactly ten. We resize the Reference
to a small range of candidate template sizes (scales of roughly
$9\times$ to $11\times$ smaller) and run the match at each. The scale whose
correlation peak is strongest wins. This makes the method robust to small
calibration differences without adding any tunable ``magic numbers''.

\subsection{Finding competing peaks}
For each scale we do not keep only the single best position. We collect the
top few correlation peaks, suppressing a small neighbourhood around each one
so that genuinely distinct positions are seen as separate candidates rather
than as one blurry blob. This lets the method \term{notice when several
positions match almost equally well} --- which, as we will see, is the crux of
the whole problem.

\subsection{Sub-pixel refinement}
Around the winning peak we fit a one-dimensional parabola along the horizontal
and vertical directions and take its apex. This nudges the integer pixel
location to a fractional one, typically improving accuracy from ``within a
pixel'' to ``a fraction of a pixel''.

\subsection{An honest self-check: the ambiguity flag}
The matcher reports not just a coordinate but also \term{how many positions
were tied for best}. If only one position stands out, it labels the result
\emph{unique}; if several compete within a small margin, it labels the result
\emph{ambiguous}. This flag turns out to be a remarkably reliable confidence
signal (Section~\ref{sec:results}).

\subsection{Choosing among tied tiles (the official rule)}
The task specifies what to do when several tiles match equally well: report the
one whose centre is \term{closest to the centre of the Search image}. The
matcher follows this rule by default. When exactly one peak stands out it simply
returns that peak; when several tie, it returns the tie nearest the frame
centre. (We expose a switch to fall back to ``just take the strongest peak'' for
comparison, but the centre rule is the spec-compliant default.)

\subsection{An optional verification stage (off by default)}
We also implemented a heavier second pass: take the handful of top candidate
positions, cut out each one from the Search image, blow it back up to the full
Reference resolution, and re-score it against the original sharp Reference.
The idea was that the extra detail might break ties. \term{We tested it and it
did not help} --- identical accuracy at three times the runtime
(Section~\ref{sec:results}). We keep it in the code, off by default, purely
for transparency: it is evidence that we tried the obvious upgrade and
measured that it does not move the metric.

% ============================ 4. THE CEILING ============================
\section{The key finding: a wall that no model can climb}
\label{sec:ceiling}

When we ran the matcher on a large synthetic test set, the results split
cleanly into two groups. About \emph{three quarters} of the samples were
localized almost perfectly. The remaining quarter failed badly --- sometimes
by hundreds of pixels. We investigated \emph{why}, because if the failures
were a weakness of NCC, a smarter model would be worth building.

\subsection{What we found}
The failing samples are \term{periodic layouts}: regular repeating arrays,
like the memory cells in a DRAM chip. In such a layout, \term{many different
positions look essentially identical}. We verified this directly: at the true
location, the degraded Search content matches the Reference \emph{no better}
than at the wrong location the matcher picked. On failing samples there were,
on average, several equally-good candidate positions; on successful samples,
essentially one.

\begin{center}
\begin{tikzpicture}[font=\footnotesize,>=Latex]
  \foreach \x in {0,1,2,3,4,5}{
    \foreach \y in {0,1,2}{
      \fill[panel,draw=klagrey] (\x*0.8,\y*0.8) rectangle ++(0.6,0.6);
      \fill[klateal!30] (\x*0.8+0.15,\y*0.8+0.15) rectangle ++(0.3,0.3);
    }}
  \draw[accent,very thick] (1.6,0.8) rectangle ++(0.8,0.8);
  \draw[klablue,very thick,dashed] (3.2,0.8) rectangle ++(0.8,0.8);
  \node[accent,right=2.7cm of {(4.8,1.2)},anchor=west,align=left]
    {\shortstack[l]{Two different positions\\ (solid vs dashed) contain\\ the \emph{same} pattern ---\\ the image cannot tell\\ them apart.}};
\end{tikzpicture}
\end{center}

\subsection{Why this is a fundamental limit, not a bug}
This is the important scientific point of our submission. When a scene truly
repeats, a \term{single} degraded Search image \emph{physically does not
contain} the information needed to decide which repeat is the right one. The
ambiguity is in the data, not in the algorithm. Consequences:

\begin{itemize}[leftmargin=1.4em]
  \item A larger neural network cannot help. There is no hidden cue for it to
        learn; the competing positions are genuinely indistinguishable in the
        given input.
  \item Our optional verification stage, which uses the full-resolution
        Reference, does not help either --- confirming the limit is about the
        Search image's content, not about our matching resolution.
  \item The honest ceiling for \emph{any} method on this data, given a single
        Search image, is roughly the fraction of samples that are non-periodic
        or whose periodic tile is uniquely anchored.
\end{itemize}

\subsection{Turning the limit into a useful signal}
Rather than hide the failures, the matcher \term{predicts them}. Its
ambiguity flag (Section~3.4) separates the two regimes almost perfectly: when
it reports a unique peak it is right about $98\%$ of the time. In a real
inspection pipeline this is exactly what an operator wants --- a localization
\emph{plus} a trustworthy ``I am confident'' / ``this region is ambiguous,
capture another view'' verdict.

% ============================ 4b. DATA GENERATOR ============================
\section{Making our own data: the synthetic generator}
\label{sec:generator}

The task rules are explicit: no dataset is provided, and each team must
\term{generate its own} reference/search pairs from publicly known structure,
never proprietary fab data. So the submission includes a small, self-contained
generator (about two hundred lines, NumPy and OpenCV only).

\subsection{What it draws}
It builds a large ``fine canvas'' at the reference resolution ($1$ nm/px), then
cuts a random $1000\times1000$ window as the Reference and shrinks the whole
canvas by $10\times$ to make the Search --- so the Reference genuinely appears,
shrunk by exactly ten, somewhere inside the Search, with the ground-truth centre
falling straight out of the crop location. Two die styles are supported (either
is judged equally):
\begin{itemize}[leftmargin=1.4em]
  \item \term{DRAM-style}: periodic horizontal word lines and vertical bit lines
        crossing at right angles, with a contact dot at a checkerboard of
        intersections.
  \item \term{FinFET-style}: dense parallel fin lines crossed by one or two
        horizontal gate bars.
\end{itemize}

\subsection{Why it needs landmarks (a lesson we learned by measuring)}
Our first generator drew a single endless periodic array. When we tested the
matcher on it, it scored near \emph{zero} --- and correctly so: with no unique
feature anywhere, every window truly is ambiguous. Real dies are not like that:
arrays are broken into blocks separated by peripheral material, and wafers carry
sparse alignment marks. We added both --- irregularly spaced separating strips
and a random ``constellation'' of alignment fiducials --- and success rose to
the $80\%$ range, with the confident subset localized perfectly. This is itself
the central finding in miniature: \term{localizability comes from unique
landmarks}, and where they are absent, no method can succeed.

\subsection{Every noise choice is justified by public literature}
The imaging model is deliberately physical, and each effect is tied to a public
source (full citations in the repository's resource log):
\begin{itemize}[leftmargin=1.4em]
  \item \term{Edge brightening}: real SEM images show brighter contrast at
        feature edges (secondary-electron edge effect).
  \item \term{Shot noise}: image intensity is an electron count, so noise grows
        with signal; controlled by an exposure ``dose''.
  \item \term{Detector/readout noise}: independent additive noise, applied
        \emph{separately} to the two captures (they are separate acquisitions).
  \item \term{Speckle}: optional multiplicative noise used to make the Search
        image noisier for the robustness test below.
\end{itemize}

\subsection{Robustness: what heavier Search noise does}
The organisers say to assume the wide-search image is noisier in the real test
set. We measured exactly that, sweeping the speckle level on $30$ freshly
generated samples each:

\begin{center}
\begin{tabular}{lccc}
\toprule
\term{Search speckle} & \term{Success@5px} & \term{Confident subset} & \term{Ambiguous subset}\\
\midrule
none ($\sigma=0.0$)   & $86.7\%$ & $22/30$ at $100\%$ & $8/30$ at $50\%$\\
moderate ($\sigma=0.3$) & $60.0\%$ & $14/30$ at $100\%$ & $16/30$ at $31\%$\\
heavy ($\sigma=0.6$)  & $36.7\%$ & $5/30$ at $100\%$ & $25/30$ at $24\%$\\
\bottomrule
\end{tabular}
\end{center}

The important column is the \term{confident subset}: it stays at
\emph{$100\%$ correct} no matter how heavy the noise. Noise does not turn good
answers into wrong ones; it \emph{shrinks the set of cases the matcher is sure
about} (fiducials get buried), and the ambiguity flag reports that honestly.
That is exactly the behaviour a real inspection tool wants under degraded
imaging.

\subsection{Slide-aligned geometric stress cases}
The generator also exposes the four geometric effects named in the organiser's
slides: charging streaks, radial barrel/pincushion scan distortion, a random
$\pm1$--$3^\circ$ Search rotation, and global feature (polygon/CD) scale from
$0.8\times$ to $1.2\times$.  The scale transform is applied about the Search
centre and the ground-truth coordinate is transformed by the same known affine
map, so it remains a valid labelled localization example.

These controls are deliberately not presented as a solved robustness claim.
The default NCC scale sweep covers the normal calibration range. A wider
synthetic scale sweep recovered an arbitrary-crop smoke case at $0.17$ px mean
error on two seeded cases when the raw maximum was used. This is a diagnostic,
not a headline result: the official centre tie-break is retained by default.
Rotation and strong radial distortion remain difficult in the SEM-like stress
generator. We implemented a rotation-corrected Search sweep and proved it on a
unique-texture geometric unit test, but it did not improve the two-image
SEM-like smoke check and costs roughly an order of magnitude more time. It is
therefore an opt-in study option, not the deployment default. This is reported
so the next iteration improves a measured weakness rather than concealing it.

\subsection{Confidence precision--recall, not a fabricated classifier CM}
Every localization manifest row contains a positive Reference/Search pair. A
normal four-cell classification confusion matrix is therefore not defined:
there are no negative pairs from which to count true negatives or false
positives. The evaluator reports the two meaningful cells at 1 px and 5 px:
within-tolerance localizations (TP) and misses (FN). For a confidence study, we
make a prediction ``accepted'' only when its NCC peak score crosses a threshold;
accepted wrong positions then become FP and rejected correct positions become FN.

The reproducible script \texttt{analysis/noise\_sweep.py} fits the threshold on
30 independent synthetic calibration pairs at each noise level and tests it on
a separate 30-pair seed. Its SVG plot is committed with the JSON ledger. At 5 px,
the held-out results were: $\sigma=0.0$, precision $79.2\%$, recall $82.6\%$;
$\sigma=0.3$, precision $92.3\%$, recall $63.2\%$; and $\sigma=0.6$,
precision $27.8\%$, recall $100\%$. The last unstable operating point is an
important negative result: heavy noise destroys the usefulness of peak-score
thresholding. All three rows are synthetic, 30-pair measurements, not official
performance.

% ============================ 5. RESULTS ============================
\section{Measured results (synthetic data)}
\label{sec:results}

\begin{center}
\fcolorbox{accent}{accent!8}{\parbox{0.95\textwidth}{\centering
\small\color{accent!60!black}
The tables below come from real runs of our matcher on $200$ held-out
samples from an external Drift-Sense \emph{synthetic} generator. They are not
official KLA/Applied Materials performance, and no setting was tuned on this
set. The generator's random-crop labels do \emph{not} use the official
closest-to-Search-centre tie convention; we therefore show the spec-compliant
run and the historical raw-peak diagnostic separately.}}
\end{center}

\vspace{0.3cm}

\begin{center}
\begin{tabular}{lccc}
\toprule
\term{Method / evaluation rule} & \term{Success@10px} & \term{Median error} & \term{Time/sample}\\
\midrule
NCC, official centre tie-break & $59.0\%$ & $1.33$ px & $209$ ms\\
Raw highest peak (legacy crop-label diagnostic, non-compliant) & $75.5\%$ & $1.03$ px & $156$ ms\\
\bottomrule
\end{tabular}
\end{center}

The official centre tie-break changes only ambiguous cases. The 59.0\% figure
is the honest spec-compliant result for this external fixture. The 75.5\%
raw-peak figure is preserved only as a diagnostic of the fixture's different
random-crop labelling convention. Fine-resolution verification previously gave
that raw diagnostic identical accuracy at about three times the runtime, so it
remains off by default.

\begin{center}
\begin{tabular}{lccc}
\toprule
\term{Subset (flagged by the matcher)} & \term{Count} & \term{Success@10px} & \term{Median error}\\
\midrule
Unique correlation peak      & $92$  & $\mathbf{97.8\%}$ & $0.83$ px\\
Competing (ambiguous) peaks  & $108$ & $25.9\%$          & $73.93$ px\\
\bottomrule
\end{tabular}
\end{center}

This second table is the heart of the submission. The spec-compliant aggregate
rate includes an easy population the method nails almost perfectly and an
ambiguous population. Because the external fixture labels arbitrary random crops
rather than the task's nearest-centre tie convention, the latter is deliberately
an adverse but honest measure. The matcher identifies this risk on its own.

% ============================ 6. WHY GOOD ENGINEERING ============================
\section{Why this is a solid engineering solution}

\begin{itemize}[leftmargin=1.4em]
  \item \term{Right-sized.} The resolvable subset is solved to roughly one-pixel accuracy by a
        classical method with two dependencies. We proved, with numbers, that
        added complexity (a verification stage) does not help --- and argued
        from the data why a learned model cannot either.
  \item \term{Honest about limits.} We identify periodic single-view ambiguity
        as a data-information limit, and separately disclose geometric
        rotation/distortion as an open, potentially solvable robustness gap.
  \item \term{Self-aware.} The ambiguity flag gives a calibrated confidence
        signal ($98\%$ correct when it claims a unique match), which is
        directly useful in a real inspection workflow.
  \item \term{Reproducible.} Fixed seeds, disjoint splits, a results ledger
        (\texttt{results/experiments.csv}), clearly labelled synthetic metrics,
        and automated tests covering the matcher's accuracy and its ambiguity
        detection.
  \item \term{Deployable.} Inference needs no internet, no GPU, and no learned
        weights: it reads a Reference and a Search image and prints the centre
        coordinates.
\end{itemize}

\vfill
\begin{center}
\color{klagrey}\small
The documented executable paths are covered by automated regression tests and
public CLI smoke runs. The result tables point to their seeded synthetic ledgers
in the accompanying repository.
\end{center}

\end{document}
