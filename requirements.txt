# Runtime dependencies for Drift-Sense localization.
# Inference (infer.py) and evaluation (evaluate.py) need only these two:
numpy>=1.24
opencv-python-headless>=4.8

# Development and reproducibility verification.
pytest>=8,<9
