# Runtime dependencies for the KLA restoration submission.
#
# The evaluator runs:  python run.py <input-dir> <output-dir>
# Only torch, numpy and Pillow are required at inference time. Install a
# CUDA-matched PyTorch wheel on the NVIDIA GPU machine before these, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# Versions verified on the reference environment (CPU wheel shown; use the
# matching CUDA build on the evaluator):
torch==2.5.1
numpy==2.4.6
Pillow==11.0.0
PyYAML==6.0.2
tqdm==4.67.1

# Evaluation-only extras (metrics, plots). run.py does NOT import these.
scikit-image==0.24.0
lpips==0.1.4
pandas==2.2.3
matplotlib==3.9.2

# Development and reproducibility verification only.
pytest==8.3.3
